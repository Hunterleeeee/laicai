# 安全说明

## 支持范围

当前只维护 `main` 分支的最新版本。安全修复会优先合入 `main`，并通过源码和本地构建脚本发布。

## 报告安全问题

请不要在公开 Issue 中粘贴 API Key、OAuth Token、Cookie、私有日志、个人路径或其它敏感信息。

如果你发现安全问题，请通过仓库平台提供的私有安全报告能力联系维护者；如果平台暂时不可用，请先只提交一个不含细节的公开说明，请维护者开启私下沟通。

## 使用者注意

来财会调用本地文件、Shell、联网搜索、浏览器和第三方模型连接器。使用前请确认：

- 只给可信任务启用高权限工具。
- 不把生产密钥写入仓库、日志或 Wiki。
- 本地 DMG 是开发者自构建产物，默认未经过 Apple notarization。
